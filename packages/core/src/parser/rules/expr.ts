import type { Expr } from "@aperio/ast";
import type { ParserState } from "../state.js";

// Expression parser entry (reserved for v2).
export function parseExpr(_state: ParserState): Expr | undefined {
  return undefined;
}
