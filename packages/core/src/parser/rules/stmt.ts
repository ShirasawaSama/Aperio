import type { Stmt } from "@aperio/ast";
import type { ParserState } from "../state.js";

// Statement parser entry (reserved for v2).
export function parseStmt(_state: ParserState): Stmt | undefined {
  return undefined;
}
